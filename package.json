{
  "name": "research-assistant",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/api-client"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @research-assistant/web",
    "build:web": "npm run build --workspace @research-assistant/web",
    "lint:web": "npm run lint --workspace @research-assistant/web",
    "typecheck:web": "npm run typecheck --workspace @research-assistant/web"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}

